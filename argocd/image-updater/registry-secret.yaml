# WARNING: demo-only placeholder.
# Do NOT commit real credentials.
#
# Prefer IRSA on EKS, or create this secret out-of-band:
#   kubectl -n argocd create secret docker-registry ecr-creds ...
apiVersion: v1
kind: Secret
metadata:
  name: ecr-creds
  namespace: argocd
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: "REPLACE_ME_BASE64"
